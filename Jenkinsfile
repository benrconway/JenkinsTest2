#!/usr/bin/env groovypipeline {
pipeline{
    agent any
    environment {
        CI='true'
    }

    stages {
        stage('Tests') {
            steps {
                sh '/usr/local/bin/npm -version'

            }
        }
      }
}
